# 07. User & Administrative Governance UI
**Status:** [PARTIALLY VERIFIED]

- **Current Implementation:** RBAC enforced via API; user profile and role rendered in top navigation bar (`admin`, `lead_engineer`, etc.).
- **Limitation:** User creation and password resets are managed via backend scripts (`bootstrap_admin.py`) and registration API; dedicated in-UI user table editor is planned.
