# 03. Knowledge Base & Document Browser
**Status:** [VERIFIED]

- **Function:** `loadKnowledgeBase()` in `frontend/index.html`.
- **Workflow:** Fetches indexed documents from `GET /api/docs` -> displays file cards with SHA-256 hashes, page counts, and indexing dates.
